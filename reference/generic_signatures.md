# Generic, cross-tissue marker signatures (broad/extended)

Generic, cross-tissue marker signatures (broad/extended)

## Usage

``` r
generic_signatures(
  species = c("mouse", "human"),
  tier = c("broad", "extended")
)
```

## Arguments

- species:

  "mouse" or "human"

- tier:

  "broad" or "extended"
