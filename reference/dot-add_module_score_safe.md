# Safe wrapper around AddModuleScore that returns the appended column names

Safe wrapper around AddModuleScore that returns the appended column
names

## Usage

``` r
.add_module_score_safe(
  obj,
  features,
  assay,
  name = "MS__",
  nbin = 24,
  ctrl = 100,
  debug_dir = NULL
)
```
